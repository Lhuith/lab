#include <iostream>

using namespace std;

// operator, perform work
main() {
    /*
    int a = 10;
    int b = 4;

    // type matters with operators!s
    cout << a + b << endl; // addition operator
    cout << a - b << endl; // sub operator
    cout << a * b << endl; // mul operator
    cout << a / b << endl; // div operator

    // 10 % 4 - 2 - remiainder 10 - 8 = 2
    cout << a % b << endl; // mod operator (cyclic operator)

    // increment ++ :       add 1
    // decrement -- :    remove 1
    */
    /*
    int c = 1;
    //c = c + 1;
    c += 1; // same as c + 1

        /= , -=, %=, *=
        out << c << endl;
    */

    int d = 1;
    cout << d-- << endl;
    cout << d << endl;

    // POSTincrement x++
    // PREincrement ++x
    // POSTdecrimentation x--
    // PREdecrimentation --x
}

