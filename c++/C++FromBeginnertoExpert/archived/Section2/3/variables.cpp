#include <iostream>

using namespace std;

// variables
// initing
// address
// allocating
// Hungarrain notation

main(){
    int A = 4;
    // A = 4;
    // & (ampersand)
    // &name_of_variable;

    cout << A << endl;
    cout << &A << endl;

    A = 10;

    cout << A << endl;
    cout << &A;
    /*
        1. can't have the same name
        2. can't start from number
        3. can't use spaces, but underscores are fine
        4. should be self descriptive
        5. cant have special characters or keywoards
        6. should be nouns
    */
}
