apiVersion: v1
kind: Service
metadata:
  name: users-service
spec:
  selector:
    app: users
  type: LoadBalancer #outside world IP reaching type
  ports:
    - protocol: TCP
      port: 8080 # outside facing port
      targetPort: 8080 # inside/internal pod port (usually 80)
