apiVersion: v1
kind: Service
metadata:
  name: tasks-service
spec:
  selector:
    app: tasks
  type: LoadBalancer #outside world IP reaching type
  ports:
    - protocol: TCP
      port: 8000 # outside facing port
      targetPort: 8000 # inside/internal pod port (usually 80)
