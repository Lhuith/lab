apiVersion: v1
kind: Service
metadata:
  name: rollout-canary-public
  namespace: canary
spec:
  type: NodePort
  ports:
    - port: 3000
      name: http
  selector:
    app: rollout-canary
---
apiVersion: v1
kind: Service
metadata:
  name: rollout-canary-stable
  namespace: canary
spec:
  type: NodePort
  ports:
    - port: 3000
      name: http
  selector:
    app: rollout-canary
---
apiVersion: v1
kind: Service
metadata:
  name: rollout-canary-preview
  namespace: canary
spec:
  type: NodePort
  ports:
    - port: 3000
      name: http
  selector:
    app: rollout-canary
